@model List<string>

@for (int i = 0; i < Model.Count; i++)
{
    <div id="discountByTime_@i" data-modal-class="discount-by-time" data-modal-control="" data-is-open="true" data-background-enable="false">
        <div class="modal-content">@Html.Raw(Model[i])</div>
    </div>
}