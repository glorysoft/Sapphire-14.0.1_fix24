@model AdvantShop.Web.Infrastructure.Admin.ShippingMethods.ElectronicDeliveryModel

@{
    var isMobile = SettingsDesign.IsMobileTemplate;
}

<div class="form-group row middle-xs setting__form-group">
    <div class="flex-grow-n setting__label @(isMobile ? "col-xs-12" : "col-fixed-size-md")">
        <span class="text-required">@T("Admin.ShippingMethods.ElectronicDelivery.PriceOfDelivery")</span>
    </div>
    <div class="col-xs-12 col-md-6 relative setting--m-t-sm @(!isMobile ? "" : "flex middle-xs")">
        @if (Model.CurrencySymbol.IsNotEmpty())
        {
            <div class="input-group setting__price-margin-in-numbers">
                @Html.TextBoxFor(x => x.ShippingPrice, new { ng_model = "shippingMethod.Params.ShippingPrice", @class = "form-control", required = "required", validation_input_text = T("Admin.ShippingMethods.ElectronicDelivery.PriceOfDelivery") })
                <div class="input-group-btn">
                    <span class="btn btn-default"><span>@Model.CurrencySymbol</span></span>
                </div>
            </div>
        }
        else
        {
            @Html.TextBoxFor(x => x.ShippingPrice, new { ng_model = "shippingMethod.Params.ShippingPrice", @class = "form-control", required = "required", validation_input_text = T("Admin.ShippingMethods.ElectronicDelivery.PriceOfDelivery") })
        }
        <help-trigger class="ng-cloak help-trigger-icon-abs @(!isMobile ? "" : "m-l-sm p-l-xs")" data-title="@T("Admin.ShippingMethods.ElectronicDelivery.PriceOfDelivery")">
            @T("Admin.ShippingMethods.ElectronicDelivery.DeliveryCostInBaseCurrency")<br><br>
            @T("Admin.ShippingMethods.ElectronicDelivery.ForExample100")<br><br>
            @T("Admin.ShippingMethods.ElectronicDelivery.IfSelfDeliveryIfFree")
        </help-trigger>
    </div>
</div>