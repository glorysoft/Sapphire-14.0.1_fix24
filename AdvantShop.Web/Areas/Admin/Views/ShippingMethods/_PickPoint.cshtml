@model AdvantShop.Web.Infrastructure.Admin.ShippingMethods.PickPointShippingAdminModel
@using AdvantShop.Shipping.PickPoint
@{
    var isMobile = SettingsDesign.IsMobileTemplate;
}

<div class="form-group row middle-xs setting__form-group">
    <div class="flex-grow-n setting__label @(isMobile ? "col-xs-12" : "col-fixed-size-md")">
        <span class="text-required">Логин</span>
    </div>
    <div class="col-xs-12 col-md-6 relative setting--m-t-sm @(!isMobile ? "" : "flex middle-xs")">
        @Html.TextBoxFor(x => x.Login, new { ng_model = "shippingMethod.Params.Login", ng_attr_type = "{{shippingMethod.inputTypeLogin}}", @class = "form-control input-alt", required = "required", validation_input_text = "Логин" })
        <a class="showhidepass @(!isMobile ? "" : "col-xs-1")" style="top:0;right:25px;" ng-init="shippingMethod.inputTypeLogin = 'password'" ng-click="shippingMethod.inputTypeLogin = shippingMethod.inputTypeLogin == 'password' ? 'text' : 'password'">
            <span class="fa" ng-class="(shippingMethod.inputTypeLogin == 'password') ? 'fa-eye' : 'fa-eye-slash'"></span>
        </a>
    </div>
</div>

<div class="form-group row middle-xs setting__form-group">
    <div class="flex-grow-n setting__label @(isMobile ? "col-xs-12" : "col-fixed-size-md")">
        <span class="text-required">Пароль</span>
    </div>
    <div class="col-xs-12 col-md-6 relative setting--m-t-sm @(!isMobile ? "" : "flex middle-xs")">
        @Html.TextBoxFor(x => x.Password, new { ng_model = "shippingMethod.Params.Password", ng_attr_type = "{{shippingMethod.inputTypePassword}}", @class = "form-control input-alt", required = "required", validation_input_text = "Пароль" })
        <a class="showhidepass @(!isMobile ? "" : "col-xs-1")" style="top:0;right:25px;" ng-init="shippingMethod.inputTypePassword = 'password'" ng-click="shippingMethod.inputTypePassword = shippingMethod.inputTypePassword == 'password' ? 'text' : 'password'">
            <span class="fa" ng-class="(shippingMethod.inputTypePassword == 'password') ? 'fa-eye' : 'fa-eye-slash'"></span>
        </a>
    </div>
</div>

<div class="form-group row middle-xs setting__form-group">
    <div class="flex-grow-n setting__label @(isMobile ? "col-xs-12" : "col-fixed-size-md")">
        <span class="text-required">Номер договора</span>
    </div>
    <div class="col-xs-12 col-md-6 relative setting--m-t-sm">
        @Html.TextBoxFor(x => x.Ikn, new { ng_model = "shippingMethod.Params.Ikn", @class = "form-control input-alt", required = "required", validation_input_text = "Номер договора" })
    </div>
</div>

@*<div class="form-group row middle-xs">
        <div class="flex-grow-n col-fixed-size-md">
            <span class="text-required">Город отправления</span>
        </div>
        <div class="col-xs-6 relative">
            @Html.TextBoxFor(x => x.FromCity, new { ng_model = "shippingMethod.Params.FromCity", @class = "form-control", required = "required", validation_input_text = "Город отправления" })
        </div>
    </div>

    <div class="form-group row middle-xs">
        <div class="flex-grow-n col-fixed-size-md">
            Регион города отправления
        </div>
        <div class="col-xs-6 relative">
            @Html.TextBoxFor(x => x.FromRegion, new { ng_model = "shippingMethod.Params.FromRegion", @class = "form-control", required = "required", validation_input_text = "Регион города отправления" })
        </div>
    </div>*@
@if (Model.Login.IsNotEmpty() && Model.Password.IsNotEmpty() && Model.Ikn.IsNotEmpty())
{
    <div class="form-group row middle-xs setting__form-group">
        <div class="flex-grow-n setting__label @(isMobile ? "col-xs-12" : "col-fixed-size-md")">
            <span class="text-required">Город отправления</span>
        </div>
        <div class="col-xs-12 col-md-6 relative setting--m-t-sm">
            @Html.DropDownListFor(x => x.FromCityId, Model.Cities, new { ng_model = "shippingMethod.Params.FromCityId", @class = "form-control input-alt", required = "required", validation_input_text = "Город отправления" })
        </div>
    </div>
}
else
{
    <div class="form-group row setting__form-group">
        <div class="col-xs-12 col-md-8">
            <div class="adv-panel-info setting__mobile-adv-panel-info" style="@(!isMobile ? "" : "display:flex;")">
                После ввода данных доступа к API, необходимо сохранить настройки после чего появятся дополнительные настройки.
            </div>
        </div>
    </div>
}

<div class="form-group row middle-xs setting__form-group">
    <div class="flex-grow-n setting__label @(isMobile ? "col-xs-12" : "col-fixed-size-md")">
        <span class="text-required">Тип сдачи отправления</span>
    </div>
    <div class="col-xs-12 col-md-6 relative setting--m-t-sm">
        @Html.DropDownListFor(x => x.GettingType, Model.GettingTypes, new { ng_model = "shippingMethod.Params.GettingType", @class = "form-control input-alt", required = "required", validation_input_text = "Тип сдачи отправления" })
    </div>
</div>

@if (Model.Login.IsNotEmpty() && Model.Password.IsNotEmpty() && Model.Ikn.IsNotEmpty() && Model.FromCityId.IsNotEmpty())
{
    <div class="form-group row middle-xs setting__form-group" ng-show="shippingMethod.Params.GettingType === '103'">
        <div class="flex-grow-n setting__label @(isMobile ? "col-xs-12" : "col-fixed-size-md")">
            <span>Пункт сдачи отправлений</span>
        </div>
        <div class="col-xs-12 col-md-6 relative setting--m-t-sm">
            @Html.DropDownListFor(x => x.FromPostamatId, Model.PostamatsSelfDelivery, new { ng_model = "shippingMethod.Params.FromPostamatId", @class = "form-control input-alt", validation_input_text = "Пункт сдачи отправлений" })
        </div>
    </div>
}
else
{
    <div class="form-group row setting__form-group" ng-show="shippingMethod.Params.GettingType === '103'">
        <div class="col-xs-12 col-md-8">
            <div class="adv-panel-info setting__mobile-adv-panel-info" style="@(!isMobile ? "" : "display:flex;")">
                После указания города отправления, необходимо сохранить настройки после чего появятся дополнительные настройки.
            </div>
        </div>
    </div>
}

<div class="form-group row middle-xs setting__form-group">
    <div class="flex-grow-n setting__label @(isMobile ? "col-xs-12" : "col-fixed-size-md")">
        <span class="text-required">Режим доставки</span>
    </div>
    <div class="col-xs-12 col-md-6 relative setting--m-t-sm">
        @Html.DropDownListFor(x => x.DeliveryMode, Model.DeliveryModes, new { ng_model = "shippingMethod.Params.DeliveryMode", @class = "form-control input-alt", required = "required", validation_input_text = "Режим доставки" })
    </div>
</div>

<div class="form-group row middle-xs setting__form-group">
    <div class="flex-grow-n setting__label @(isMobile ? "col-xs-12" : "col-fixed-size-md")">
        <span class="text-required">Отображение ПВЗ</span>
    </div>
    <div class="col-xs-12 col-md-6 relative setting--m-t-sm">
        @Html.DropDownListFor(x => x.TypeViewPoints, Model.ListTypesViewPoints, new { ng_model = "shippingMethod.Params.TypeViewPoints", @class = "form-control input-alt", required = "required", validation_input_text = "Отображение ПВЗ" })
    </div>
</div>

<div class="form-group row middle-xs setting__form-group" ng-if="shippingMethod.Params.TypeViewPoints === '@(((int)TypeViewPoints.YaWidget).ToString())'">
    <div class="flex-grow-n setting__label @(isMobile ? "col-xs-12" : "col-fixed-size-md")">
        <span class="text-required">API-ключ яндекс.карт</span>
    </div>
    <div class="col-xs-12 col-md-6 relative setting--m-t-sm @(!isMobile ? "" : "flex middle-xs")">
        @Html.TextBoxFor(x => x.YaMapsApiKey, new { @class = "form-control input-alt", ng_attr_type = "{{shippingMethod.inputTypeApiKey}}", ng_model = "shippingMethod.Params.YaMapsApiKey", required = "required", validation_input_text = "API-ключ яндекс.карт" })
        <a class="showhidepass @(!isMobile ? "" : "col-xs-1")" style="top:0;right:25px;" ng-init="shippingMethod.inputTypeApiKey  = 'password'" ng-click="shippingMethod.inputTypeApiKey  = shippingMethod.inputTypeApiKey  == 'password' ? 'text' : 'password'">
            <span class="fa" ng-class="(shippingMethod.inputTypeApiKey  == 'password') ? 'fa-eye' : 'fa-eye-slash'"></span>
        </a>
        <help-trigger class="ng-cloak help-trigger-icon-abs @(!isMobile ? "" : "m-l-sm p-l-xs")" data-title="API-ключ яндекс.карт">
            <a href="@LinkService.SocialMedia.Yandex/dev/maps/jsapi/doc/2.1/quick-start/index.html#get-api-key" target="_blank">Как получить ключ?</a>
        </help-trigger>
    </div>
</div>

<div class="form-group row middle-xs" ng-if="shippingMethod.Params.TypeViewPoints === '@(((int)TypeViewPoints.YaWidget).ToString())'">
    @if (!isMobile)
    {
        <div class="col-xs-11 flex-grow-n col-fixed-size-md setting__label setting--m-none">
            <label for="ShowAddressComment">Отображать описание схемы проезда</label>
        </div>
    }
    <div class="col-xs-12 col-md-6 relative">
        <label class="adv-checkbox-label form-label-block">
            @Html.CheckBoxFor(x => x.ShowAddressComment, new { ng_model = "shippingMethod.Params.ShowAddressComment", ng_init = "shippingMethod.Params.ShowAddressComment=" + Model.ShowAddressComment.ToLowerString(), @class = "adv-checkbox-input" })
            <span class="adv-checkbox-emul"></span>
            @if (isMobile)
            {
                <span>Отображать описание схемы проезда</span>
            }
        </label>
    </div>
</div>

@Html.Partial("_SyncStatuses", Model)
