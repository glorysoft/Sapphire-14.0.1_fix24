@model AdvantShop.Web.Admin.Models.Landings.LandingAdminSiteSettings

@{ 
    var isMobile = SettingsDesign.IsMobileTemplate;
}

<div class="row">
    <div class="col-xs-12 col-md-11">
        <div class="form-group row middle-xs">
            <div class="col-xs-12 col-md-3 col-wl-2">
                <div class="setting-label-wrap @(isMobile ? "m-b-sm funnel__label" : "m-b-md")">
                    @T("Admin.Views.Funnels.Settings.Common.FunnelName")
                </div>
            </div>
            <div class="col-xs-12 col-md-9 relative">
                @Html.TextBoxFor(x => x.SiteName, new { @class = "form-control input-alt", ng_model = "landingSite.settings.SiteName", required = "required", validation_input_text = "Название сайта" })
                <div class="@(isMobile ? "m-t-xs fs-xs" : "")"><i>@T("Admin.Views.Funnels.Settings.Common.FunnelNameExample")</i></div>
            </div>
        </div>

        @*<div class="form-group row middle-xs">
            <div class="col-xs-3 col-wl-2">
                <div class="setting-label-wrap">
                    Опубликован
                </div>
            </div>
            <div class="col-xs-9 relative">
                <label class="adv-checkbox-label" data-e2e="funnelEnabled">
                    @Html.CheckBoxFor(x => x.SiteEnabled, new { @class = "adv-checkbox-input", ng_model = "landingSite.settings.SiteEnabled" })
                    <span class="adv-checkbox-emul"></span>
                </label>
                <help-trigger class="ng-cloak m-l-xs" data-title="Опубликован">
                    <div class="help-content">
                        Когда воронка опубликована, ее видят все пользователи и появляется в карте сайта. Иначе воронку видит только администратор.
                    </div>
                </help-trigger>
            </div>
        </div>*@
    </div>
</div>


